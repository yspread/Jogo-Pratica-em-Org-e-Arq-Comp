#include <stdio.h>
#include <stdlib.h>

/*void print_string(char* str, int offset){
    for(int i = 0; str[i] != '\0'; i++){
        _outchar(str[i], i + offset);
    }
}*/


int main(){
    int r = rand();
    printf("%d\n", r);
}